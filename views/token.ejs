<!DOCTYPE html>
<html lang="en">
<% include partials/head.ejs %>

<body style="font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif">
        <div id="logmain">
                        <div class="login">
                                    <div class="logpage">
                                        <p>A token has been sent to your email/phone number. Look it up and input it into the form below</p>
                                        <% if (thetoken.length > 0){ %>
                                            <i>TOKEN : <span class="msg1"><%= thetoken%></span></i>
                                        <% } %>
                                            <form id="tokenform" action="/target" method="POST">
                                                <div class="form-group">
                                                    <label for="token">Enter token</label>
                                                    <input name="token" class="form-control" value="<%= thetoken %>" required id="tok">
                                                </div>
                                                <button type="submit" class="btn btn-default btn-success btn-block next">Submit</button>
                                            </form>
                                            <p>Didn't receive any code? <a href="#" style="color:red" class="resend">Resend token</a></p>
                                            <p><i>Token expires in five minutes</i></p>
                                            <a class="dash-name" href="/login" style="color:red">Login here</a>
                                        </div>
                            
                        </div>        
        </div>
        
    </body>

</html>