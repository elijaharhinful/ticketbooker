<!DOCTYPE html>
<html lang="en">
<% include partials/head.ejs %>

    <body style="font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif">
        <div id="logmain">
                        <div class="login">
                                <div class="logpage">
                                    <h4>Login|
                                        <a class="dash-name" href="/sign" style="color:red">Register here</a>
                                    </h4>
                                    <form id="loginform" class="modal-form" action="/auth" method="POST">
                                        <% if (message.length > 0){ %>
                                            <i><span class="msg"><%= message %></span></i>
                                        <% } %>
                                        <% if (message1.length > 0){ %>
                                            <i><span class="msg1"><%= message1 %></span></i>
                                        <% } %>
                                        <div class="form-group">
                                            <label for="tel">Mobile number</label>
                                            <input type="tel" name="tel" class="form-control num" required id="tel"  minlength="10" maxlength="10" placeholder="example: 0240000000">
                                        </div>
                                        
                                        <div class="form-group">
                                            <label for="psw">Password</label>   
                                            <input type="password" name="password" class="form-control passwd" id="psw" minlength="6" required>
                                        </div>
                                        
                                        <div class="checkbox">
                                            <label>
                                                <input type="checkbox" name="checkbox" value="Remember me">Remember me
                                            </label>
                                        </div>
                                        <button type="submit" class="btn btn-sm btn-primary">Login</button>
                                    </form>
                                    <div class="modal-footer">
                                        <p>
                                            <a href="/passreset" style="color: red;">Forgot Password?</a>
                                        </p>
                                    </div>
                                </div>
                            
                        </div>        
        </div>
    </body>

</html>